# Shark Tank AI — requirements
# Core runtime dependencies needed for the app to start successfully,
# plus dev/test tooling.

# --- UI ---
streamlit>=1.37

# --- Configuration ---
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0

# --- LLM provider (not yet wired to any agent logic) ---
anthropic>=0.34
httpx>=0.27

# --- PDF proposal intake (utils/pdf_extraction.py, Release 0.6) ---
pypdf>=4.0

# --- Founder Feedback Report PDF rendering (utils/report_rendering.py, Release 0.9) ---
reportlab>=4.0

# --- Testing / dev tooling ---
pytest>=8.2
pytest-mock>=3.14
ruff>=0.5
black>=24.4
mypy>=1.10
